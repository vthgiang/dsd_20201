import styled from "styled-components";

export default styled.div`
  cursor: pointer;
  display: flex;
  align-items: center;
  height: 64px;
  padding: 16px;
  .fa-drone-alt {
    color: rgba(255, 255, 255, 0.9);
    font-size: 32px;
  }
  .title {
    padding-left: 7.5px;
    color: #fff;
    font-weight: 700;
  }
`;
